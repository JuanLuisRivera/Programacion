/*Ejemplo del funcionamiento basico de caracteres
 * Juan Luis Rivera
 * 21 de Enero 2024*/

#include <stdio.h>
#include <stdlib.h>


int main (int argc, char** argv){

    //We define a variable datatype "char" with the name "menuChoice" and initialize it with a value of c
    char menuChoice = 'c'; //For the char datatype we use "''" (single quotes) instead of """" (double quotes)

    menuChoice = 120; //We assign a new value character using the ASCII integer table

    return (EXIT_SUCCESS);
}